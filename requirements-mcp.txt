# Dependencies for the native MCP stdio server (mcp_server.py) only.
#
# Kept separate from requirements.txt so the deployed API image stays lean and
# so the MCP SDK's transitive pins (it pulls a newer starlette) never collide
# with the FastAPI app's. Install into its own environment:
#   python -m venv .venv-mcp && . .venv-mcp/bin/activate
#   pip install -r requirements-mcp.txt
mcp>=1.2
httpx>=0.27
